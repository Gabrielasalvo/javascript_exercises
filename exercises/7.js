
function baskara(ax2,bx,c) {
    // let randomizar = function(){Math.random(result)}

    if(c < 0) {
        console.log("Delta é negativo. Tente um valor positivo" )
        
    }else{

        return (
            result = Math.pow(ax2, 2) + bx + c
            
            
        )
        result.forEach(Math.random(result))
        
    }

}

console.log(baskara(2,2,2))